dhmf:
  config_path: example/a/config_open.yaml
  query_mode: dual_path

paths:
  db_path: example/a/DB/main.db
  output_dir: benchmark_results
  questions_path: null
  report_path: null    # 评测报告 JSON，留空则 {output_dir}/eval_report_时间戳.json
  summary_path: null   # 综合统计表 txt；留空则与 report 同名的 .summary.txt
  dataset_path: null

generate:
  hop_counts:
    "1": 50
    "2": 0
    "3": 0
  seed: 42
  max_chars_per_doc: -1
  max_retries: 3
  sleep_between: 0.0
  use_cache: false
  api_key: null   # 出题 LLM 端点；留空则继承 dhmf.config → retrieve → settings
  base_url: null
  timeout: 180
  model_args:
    model: null  # model 留空则继承 dhmf retrieve.model_args.model
    temperature: 0.2
    enable_thinking: false
    # max_tokens: 1024
    response_format:
      type: json_object

evaluate:
  enable_doc_recall: False
  max_source_chars: -1
  max_retries: 3
  sleep_between: 0.0
  save_summary_txt: true  # 是否写 .summary.txt
  use_cache: false  # RAG 回答 LLM 是否走缓存（一般关，保证评测可复现对比）
  dhmf_retrieve_use_cache: null  # 是否覆盖 dhmf 侧 retrieve.use_cache（null=不改配置）
  api_key: null  # 评判 LLM 端点；留空则继承 dhmf retrieve
  base_url: null
  timeout: 180
  model_args:
    model: null
    temperature: 0.0
    enable_thinking: false
    # max_tokens: 512
    response_format:
      type: json_object

logging:
  level: INFO  # DEBUG / INFO / WARNING / ERROR
